type Car {
  id: ID!
  type: String!
  model: String!
  location: String!
}

input CarFilter {
  type: String
  model: String
  location: String
}

type Query {
  searchCars(filter: CarFilter): [Car!]!
}
